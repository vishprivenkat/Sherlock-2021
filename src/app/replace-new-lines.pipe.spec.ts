import { ReplaceNewLinesPipe } from './replace-new-lines.pipe';

describe('ReplaceNewLinesPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceNewLinesPipe();
    expect(pipe).toBeTruthy();
  });
});
